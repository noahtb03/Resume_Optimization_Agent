# BYOK: your own Anthropic API key. The CLI reads this; core/service code never does.
# Copy to .env for local convenience, OR pass --api-key on the command line.
# Never commit your real key.
ANTHROPIC_API_KEY=sk-ant-your-key-here
